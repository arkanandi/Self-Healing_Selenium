[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
    -ra
markers =
    smoke: smoke tests
    regression: regression tests
    ui: UI tests
    slow: slow running tests